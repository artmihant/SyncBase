# Sync.py - Управление базой знаний

Программа для управления всей базой знаний и синхронизации с Яндекс.Облаком.

## Команды

### `sync_project.py status`
Показывает детальный статус конкретного проекта.

**Вывод:**
- Информация о кэше проекта
- Список новых файлов и папок
- Список удаленных файлов и папок  
- Список измененных файлов с деталями изменений
- Рекомендации по синхронизации

**Пример:**
```bash
$ python3 sync_project.py status
📊 Статус проекта <app:/Orgs/KnowledgeBase>...
✅ Кэш найден (обновлен: 2025-08-08T13:47:19.065946)
🔄 Обнаружены изменения:

📁 Новые файлы (774):
   + .git/COMMIT_EDITMSG (7.0 B)
   + .git/FETCH_HEAD (0 B)
   ...

📂 Новые папки (131):
   + .git/
   + .git/branches/
   ...

📝 Измененные файлы (3):
   ~ .project_cache.json
     время: 2025-08-08 → 2025-08-08
   ~ sync.py
     размер: 30341 → 37457
     время: 2025-08-08 → 2025-08-08

💡 Выполните 'sync_project.py save' для синхронизации изменений
```

### `sync.py status`
Быстрый обзор состояния всех проектов в базе знаний.

**Вывод:**
- Количество проектов
- Статус каждого проекта (✅ синхронизирован, 🔄 требует синхронизации)
- Общая статистика (размер, количество файлов)

**Пример:**
```bash
$ python3 sync.py status
📊 Статус базы знаний...
🔍 Найдено проектов: 11

📋 Статус проектов:
--------------------------------------------------------------------------------
✅ Articles/ArticleFullWaveSeis             4660 файлов     962.7 MB
🔄 Articles/ArticleVerDegSeis               кэш отсутствует
...
```

### `sync.py list`
Подробный список всех проектов с информацией о каждом.

**Вывод:**
- Нумерованный список проектов
- Статус синхронизации
- Время последнего изменения
- Количество файлов и размер

**Пример:**
```bash
$ python3 sync.py list
📋 Список проектов в базе знаний:
--------------------------------------------------------------------------------
 1. ✅ Articles/ArticleFullWaveSeis
     📅 Последнее изменение: 2025-08-08 12:32
     📊 Файлов: 4660, Размер: 962.7 MB
...
```

### `sync.py all`
Синхронизация всех проектов, требующих обновления.

**Функции:**
- Показывает список проектов, требующих синхронизации
- Запрашивает подтверждение
- Выполняет синхронизацию каждого проекта

**Пример:**
```bash
$ python3 sync.py all
🔄 Синхронизация всех проектов...
🔄 Проекты, требующие синхронизации: 9
   - Articles/ArticleVerDegSeis
   - Coworks/Larionov
   ...

❓ Продолжить синхронизацию? (y/N): y
🚀 Начинаем синхронизацию...
```

## Как это работает

### Кэширование
- Каждый проект создает файл `.project_cache.json` при синхронизации
- Кэш содержит информацию о файлах, их размерах, MD5 хешах и статусах синхронизации
- Кэш автоматически синхронизируется с облаком

### Определение статуса проекта
- **✅ Синхронизирован** - есть кэш и файлы, нет конфликтов
- **🔄 Требует синхронизации** - нет кэша или есть файлы с различающимися MD5
- **📁 Пустой** - проект без файлов

### Обнаружение проектов
Проект считается валидным, если содержит хотя бы один из файлов:
- `VERSION`
- `.syncignore`
- `README.md`
- `package.json`
- `requirements.txt`
- `Pipfile`
- `Dockerfile`

Или содержит подпапки:
- `src`, `docs`, `tests`, `assets`, `images`

## Требования

- Python 3.6+
- Переменные окружения в `.env`:
  - `YANDEX_DISK_TOKEN` - токен для доступа к Яндекс.Диску
  - `BASE_PATH` - путь к корневой папке базы знаний

## Использование

### Управление всей базой знаний:
```bash
# Перейти в папку KnowledgeBase
cd /path/to/Base/Orgs/KnowledgeBase

# Показать статус всех проектов
python3 sync.py status

# Показать список проектов
python3 sync.py list

# Синхронизировать все проекты
python3 sync.py all
```

### Управление отдельным проектом:
```bash
# Перейти в папку проекта
cd /path/to/Base/Articles/MyArticle

# Показать детальный статус проекта
python3 sync_project.py status

# Сохранить изменения в облако
python3 sync_project.py save

# Загрузить изменения из облака
python3 sync_project.py load
``` 